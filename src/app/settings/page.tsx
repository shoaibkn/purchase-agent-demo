export default function SettingsPage() {
  return (
    <section className="space-y-3">
      <h1 className="text-3xl font-semibold">Settings</h1>
      <p className="text-[color:var(--muted)]">Model switching and simulator mode toggles will be configured here.</p>
    </section>
  );
}
